{
  "name": "@fulleststack/ui",
  "type": "module",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "rm -rf dist && tsc --watch",
    "build": "rm -rf dist && tsc",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@fulleststack/eslint-config": "workspace:^",
    "@types/node": "workspace:*",
    "@types/react": "workspace:*",
    "typescript": "workspace:*"
  }
}
